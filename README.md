# local-code-agent

*Read this in [English](README.en.md).*

Локальний кодинг-агент на **gemma4:26b-a4b-it-qat** (через Ollama), спроєктований
під слабку локальну модель: вузький контекст, декомпозиція задачі, агентний
tool-loop і детерміноване поводження з великими даними.

## Ідея

- **Planner** (think=on) — читає задачу й ріже її на дрібні кроки.
- **Per-step tool-loop** (think=off, вузький контекст) — на кожен крок модель
  через нативний tool-calling сама викликає інструменти (читати/писати/редагувати
  файли, запускати команди), а рантайм їх виконує й повертає результат, поки крок
  не завершено. Модель ніколи не виконує код сама — це робить наш цикл.
- **Strip/Restore** — великі опакові літерали (напр. протобафи в коді) детерміновано
  вирізаються в плейсхолдери *до* LLM і повертаються байт-у-байт *після*. Модель їх
  не бачить → не псує, а контекст не роздувається.

## Чому не Aider

Aider завжди кладе весь файл у контекст і регенерує його цілком — на токен-щільних
файлах слабка локальна модель захлинається й псує дані. Тут великі дані взагалі не
йдуть крізь модель.

## Реєстр інструментів

Усе виконання уніфіковане під tool-calling. Інструмент = `{схема, обробник}`;
додати новий = зареєструвати його в реєстрі, без змін у циклі. Базові інструменти:

| Інструмент | Призначення |
|---|---|
| `list_dir` | структура файлів проєкту |
| `read_file` | прочитати файл |
| `write_file` | створити / перезаписати файл |
| `edit_file` | відрефакторити наявний `.py` (зі strip/restore) |
| `create_from_source` | створити НОВИЙ файл як копію джерела (зі strip/restore) |
| `run_shell` | запустити програму (python / pytest / git) |
| `read_attachment` | прочитати файл, прикріплений користувачем (нечіткий пошук за іменем) |
| `web_search` | пошук у вебі (DuckDuckGo через `ddgs`, без ключа) |

Усі шляхи файлових тулів обмежені коренем проєкту (захист від traversal через `..`
чи абсолютний шлях).

## Дозволи

- **правки** (`ask` / `auto`) — `ask` показує план і чекає підтвердження;
  `auto` виконує одразу.
- **консоль** (`smart` / `ask` / `auto` / `allowlist` / `off`):
  - `smart` (дефолт) — безпечні команди виконуються авто, потенційно руйнівні
    (`rm -rf`, форматування, системні теки тощо — див. `shell_guard.py`) зупиняються
    на підтвердження;
  - `ask` — питає попапом перед кожною командою;
  - `auto` — виконує все без питань;
  - `allowlist` — лише безпечні префікси з білого списку;
  - `off` — вимикає shell.

## Структура

```
agent/
  config.py      профілі EXECUTOR / PLANNER
  llm.py         клієнт Ollama (tool-calling, think-перемикач)
  literals.py    strip / restore великих літералів
  tools.py       детерміновані файлові/shell примітиви + allow-list
  executor.py    run_edit / create_from_source (strip→LLM→restore→diff)
  toolkit.py     реєстр інструментів + ToolContext (root, дозволи, confirm)
  agent_loop.py  per-step tool-loop (run_step)
  planner.py     декомпозиція задачі на кроки
  intent.py      класифікація наміру (edit / plan / answer / shell)
  answerer.py    відповіді/аналіз коду (think=on)
  memory.py      стан задачі на диск
  session.py     сесії/чати (проєкт, дозволи, історія, джерела)
  project.py     AGENT.md (знання проєкту) + скан структури
  convo.py       контекст-памʼять розмови (стислий підсумок між запитами)
  attachments.py вкладення на диск + read_attachment
  shell_guard.py класифікатор небезпечних shell-команд (режим smart)
  websearch.py   веб-пошук (DuckDuckGo, провайдер змінний)
  settings.py    налаштування (тема, розміри шрифтів) на диск
lca_web/
  lca_web.py     веб-інтерфейс (Reflex, стиль Claude); режими Чат | Код
main.py          точка входу (зручно з PyCharm)
rxconfig.py      конфіг Reflex
```

## Надійність під слабку модель

Кілька прийомів, що тримають слабку локальну модель у межах робочого циклу:

- **DRY-семплінг** у профілі `EXECUTOR` — карає повтор цілих фраз/послідовностей
  (напр. та сама невдала команда раз за разом), не псуючи код, як грубий
  `repeat_penalty`.
- **Tool-call nudge** — якщо модель «розповідає» намір замість виклику тула, цикл
  один раз підштовхує її виконати крок дією.
- **Контекст плану в кожен крок** — виконавець кроку бачить загальну задачу й сусідні
  кроки, а не лише голий опис свого кроку.
- **Strip/Restore** великих літералів (див. вище) + `think=off` для структурованого
  виводу (JSON-плани не обриваються думанням).

## Знання проєкту (AGENT.md)

У корені робочого проєкту може лежати `AGENT.md` — куроване знання, що інжектиться
планувальнику/виконавцю на кожну задачу (не RAG/ембединги — для малого проєкту й
слабкої моделі один курований файл працює краще). Опційний init-scan авто-чернетує
`AGENT.md` для нового репозиторію.

## Вимоги

- Python 3.12, `requests`, `reflex==0.9.5.post2`, `ddgs` (веб-пошук)
- Ollama з моделлю `gemma4:26b-a4b-it-qat`

## Ollama: q4_0 KV-cache + flash attention

За бенчмарком (gemma4 / qwen3-coder, контекст 65536→131072→262144) `q4_0` KV-cache +
flash attention дають майже безкоштовне подвоєння контексту (gemma4 113.5→108.5 t/s,
-4%) і помітно менший VRAM (KV-буфер падає в ~3.5 рази: 12288 MiB → 3456 MiB на
131072). Тому `agent/llm.py` `_ensure_server` піднімає `ollama serve` з
`OLLAMA_FLASH_ATTENTION=1` і `OLLAMA_KV_CACHE_TYPE=q4_0` у env — для БУДЬ-якої моделі,
без правок per-model.

**Пастка на Windows**: десктоп-трей `ollama app.exe` має власний наглядач, який
перезапускає сервер зі своїм (старим) env-блоком — зміна User env vars через
`setx`/System Properties не доходить до вже запущеного наглядача без повного
перелогіну/перезавантаження. Якщо трей-апка встигає піднятись першою, наш env
просто ігнорується.

Тому: автостарт `ollama app.exe` вимкнено (ярлик з `...\Startup\Ollama.lnk` прибрано
в `%USERPROFILE%\Ollama_autostart_disabled.lnk`) — сервер тепер піднімає лише наш
код (`_ensure_server`, явний `env=` у `subprocess.Popen`, незалежно від системного
реєстру). Перший запуск `python main.py` після перезавантаження машини стартує
Ollama сам; трей-іконки більше не буде.

## Запуск

```powershell
# веб-інтерфейс (фронтенд :3000, бекенд :8000)
.\run_web.ps1
# або
python main.py
```

З PyCharm: відкрий `main.py` → ▶ Run (інтерпретатор — `.venv` проєкту).

## Тести

Усі тести офлайн (стаб-клієнти, без GPU):

```powershell
.venv\Scripts\python.exe -m tests.test_toolkit
.venv\Scripts\python.exe -m tests.test_agent_loop
# ... та інші в tests/
```
